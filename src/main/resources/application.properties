spring.application.name = project1_1

server.port = 5678

# Database Credentials
spring.datasource.url = jdbc:postgresql://p-1.c1m0oqqa6rcx.us-east-2.rds.amazonaws.com:5432/postgres
spring.datasource.username = postgres
spring.datasource.password = password

# Spring Data Settings

# This allows us to see SQL running in the console whenever a DB operation happens
spring.jpa.show-sql = true

# Setting our DDL to update the DB when a change in the model classes happens
spring.jpa.hibernate.ddl-auto = update

# Specify the schema we're using
spring.jpa.properties.hibernate.default_schema = project1_1